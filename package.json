{
  "name": "playwright-academy",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "pretest": "echo 'About to run the tests...'",
    "test": "playwright test tests/contactListTests.spec.ts",
    "posttest": "echo 'Tests completed!'",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "test:report": "npm run test:smoke ; npm run report",
    "test:chromium": "playwright test --project=chromium",
    "test:smoke": "playwright test --grep @smoke",
    "test:dev": "BASE_URL=http:/localhost:5000 playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.8.0",
    "dotenv": "^17.4.2"
  }
}